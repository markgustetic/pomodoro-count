## What does this change?

<!-- One or two sentences. If it fixes an issue, add "Fixes #123". -->

## Why?

<!-- The problem behind the change. Skip if it's obvious from the above. -->

## How was it verified?

<!-- "just test" alone is fine for logic. For UI changes, say what you actually
     looked at — a `just preview` render, or the running app. -->

## Checklist

- [ ] `just test` passes
- [ ] `just build` produces a working app
- [ ] Tests added or updated for the behaviour that changed
- [ ] `CHANGELOG.md` updated under **Unreleased** (skip for internal-only changes)
